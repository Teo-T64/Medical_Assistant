spring:
  application:
    name: symptom
  mongodb:
      uri: ${URI}

server:
    port: 8082
services:
  user-service:
    url: ${USER_SERVICE_URL}
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL}